#!/bin/bash
set -ix
exec >>/var/log/fastd-verifylog 2>&1
# TODO hier irgendetwas schlaues hinschreiben, dass erstmal eine blacklist
# prüft und ansonsten immer erfolgreich beendet wird
key=$1

if grep -q "$key" /etc/fastd/blacklist/blacklist
then
  exit 1
else
  exit 0
fi
